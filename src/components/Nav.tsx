import {Activity,Home,Plus,UserRound,UsersRound} from 'lucide-react'; import {go,parseHash} from '../lib/router';
export function BottomNav({inside=false,onAdd}:{inside?:boolean;onAdd:()=>void}){const vibeId=parseHash().id;return <nav className="bottom-nav" aria-label="Main navigation"><button onClick={()=>go(inside&&vibeId?`/vibe/${vibeId}`:'/')}><Home/><span>{inside?'Overview':'Home'}</span></button><button onClick={()=>go(inside&&vibeId?`/vibe/${vibeId}/members`:'/vibes')}><UsersRound/><span>{inside?'Mates':'Vibes'}</span></button><button className="nav-add" onClick={onAdd} aria-label={inside?'Add expense':'Create vibe'}><Plus/></button><button onClick={()=>go(inside&&vibeId?`/vibe/${vibeId}/activity`:'/activity')}><Activity/><span>Activity</span></button><button onClick={()=>go('/profile')}><UserRound/><span>Profile</span></button></nav>}
