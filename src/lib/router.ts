export type Route={page:string;id?:string;token?:string};
export function parseHash(hash=location.hash):Route{const path=hash.replace(/^#\/?/,'').split('/').filter(Boolean);if(!path.length)return{page:'home'};if(path[0]==='create')return{page:'create'};if(path[0]==='join')return{page:'join',token:path[1]};if(path[0]==='profile')return{page:'profile'};if(path[0]==='activity')return{page:'global-activity'};if(path[0]==='vibes')return{page:'vibes'};if(path[0]==='vibe')return{page:path[2]||'vibe',id:path[1]};return{page:'home'}} export const go=(path:string)=>{location.hash=path};
