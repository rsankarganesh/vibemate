import {useEffect,useState} from 'react'; import {ChevronRight,ShieldCheck} from 'lucide-react'; import {Logo} from './components/Logo'; import {parseHash} from './lib/router'; import LiveApp from './LiveApp';
const KEY='vibemate-display-name';
export default function LiveRoot(){const [name,setName]=useState(localStorage.getItem(KEY)||''),[draft,setDraft]=useState('');const joining=parseHash().page==='join';useEffect(()=>{if(!name)return;const update=()=>{const heading=document.querySelector('.welcome h1');if(heading?.textContent?.includes('Hey, Alex'))heading.textContent=`Hey, ${name} 👋`};update();const observer=new MutationObserver(update);observer.observe(document.body,{childList:true,subtree:true});return()=>observer.disconnect()},[name]);if(!name&&!joining)return <main className="welcome-page profile-onboarding"><Logo inverse/><div className="welcome-copy"><p className="eyebrow">WELCOME TO VIBEMATE</p><h1>What should your mates call you?</h1><p>This name will appear inside the vibes you create. No Google account required.</p><form onSubmit={e=>{e.preventDefault();const clean=draft.trim();if(clean.length<2)return;localStorage.setItem(KEY,clean);setName(clean)}}><label className="onboarding-label">Your name<input autoFocus required minLength={2} maxLength={60} value={draft} onChange={e=>setDraft(e.target.value)} placeholder="e.g. Sankar"/></label><button className="primary light full">Continue <ChevronRight/></button></form></div><p className="private-note"><ShieldCheck/>Your access is remembered on this browser</p></main>;return <LiveApp/>}
