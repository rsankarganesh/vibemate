import {useEffect,useState} from 'react'; import {ArrowLeft,Check,ChevronRight,Plus,Share2,ShieldCheck} from 'lucide-react';
import {Logo} from './components/Logo'; import {VibeCard} from './components/VibeCard'; import {ExpenseForm} from './components/ExpenseForm'; import {Modal} from './components/Modal'; import {calculateBalances,suggestSettlements} from './lib/finance'; import {formatMoney} from './lib/money'; import {getMemberships,type StoredMembership} from './lib/storage'; import {go,parseHash} from './lib/router'; import {createLiveVibe,joinLiveVibe,loadLiveVibe,previewInvite,saveLiveExpense,settleLive} from './services/vibe-service'; import type {Expense,Vibe} from './types';
const Header=({back=false}:{back?:boolean})=><header className="topbar">{back?<button className="icon-btn" onClick={()=>history.back()} aria-label="Go back"><ArrowLeft/></button>:<Logo/>}<span className="demo-badge live-badge">Live</span><button className="avatar">AL</button></header>;
const Loading=()=> <main className="page"><div className="empty"><h2>Loading your vibes…</h2></div></main>;
export default function LiveApp(){const [route,setRoute]=useState(parseHash()),[vibes,setVibes]=useState<Vibe[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[modal,setModal]=useState(false),[toast,setToast]=useState('');const refresh=async()=>{setLoading(true);try{const loaded=await Promise.all(getMemberships().map(loadLiveVibe));setVibes(loaded);setError('')}catch(e){setError(e instanceof Error?e.message:'Couldn’t load your vibes')}finally{setLoading(false)}};useEffect(()=>{void refresh();const h=()=>setRoute(parseHash());addEventListener('hashchange',h);addEventListener('focus',refresh);return()=>{removeEventListener('hashchange',h);removeEventListener('focus',refresh)}},[]);const notify=(s:string)=>{setToast(s);setTimeout(()=>setToast(''),2200)};if(route.page==='create')return <CreateLive onDone={async m=>{await refresh();go(`/vibe/${m.vibeId}`)}}/>;if(route.page==='join')return <JoinLive token={route.token||''} onDone={async m=>{await refresh();go(`/vibe/${m.vibeId}`)}}/>;if(loading)return <><Header/><Loading/></>;if(error)return <><Header/><main className="page"><div className="empty"><h2>Couldn’t load VibeMate</h2><p>{error}</p><button className="primary" onClick={refresh}>Try again</button></div></main></>;const vibe=vibes.find(v=>v.id===route.id);if(vibe){const me=calculateBalances(vibe.members.map(m=>m.id),vibe.expenses,vibe.settlements).find(b=>b.memberId==='alex')!;const transfers=suggestSettlements(calculateBalances(vibe.members.map(m=>m.id),vibe.expenses,vibe.settlements));const membership=getMemberships().find(m=>m.vibeId===vibe.id)!;return <><Header back/><main className="page vibe-page"><section className="vibe-hero"><div className="vibe-title"><span>{vibe.emoji}</span><div><p className="eyebrow">{vibe.type}</p><h1>{vibe.name}</h1></div></div><p>{vibe.when} · {vibe.location}</p><button className="share" onClick={()=>{navigator.clipboard.writeText(`${location.origin}${location.pathname}#/join/${vibe.inviteToken}`);notify('Invite link copied ✓')}}><Share2/> Copy invite</button></section><section className="balance-feature"><p>Your balance</p><h2>{me.balanceCents>=0?'You’re owed':'You owe'} {formatMoney(Math.abs(me.balanceCents))}</h2><div><span><small>You paid</small><strong>{formatMoney(me.paidCents)}</strong></span><span><small>Fair share</small><strong>{formatMoney(me.shareCents)}</strong></span></div></section><button className="primary full add-expense" onClick={()=>setModal(true)}><Plus/> Add expense</button><section><div className="section-heading"><h2>Expenses</h2></div><div className="expense-list">{vibe.expenses.filter(e=>!e.deletedAt).reverse().map(e=><article className="expense-row" key={e.id}><span className="expense-icon">🧾</span><div><strong>{e.description}</strong><p>Paid by {vibe.members.find(m=>m.id===e.paidBy)?.name}</p></div><span><strong>{formatMoney(e.amountCents)}</strong><small>{e.date}</small></span></article>)}{!vibe.expenses.length&&<div className="empty"><p>No expenses yet. Someone has to buy the first round!</p></div>}</div></section><section><div className="section-heading"><h2>Settle up</h2></div>{transfers.map((t,i)=><article className="settlement" key={i}><div><strong>{vibe.members.find(m=>m.id===t.fromMemberId)?.name} pays {vibe.members.find(m=>m.id===t.toMemberId)?.name}</strong><h2>{formatMoney(t.amountCents)}</h2></div><button className="secondary" onClick={async()=>{await settleLive(membership,t.fromMemberId,t.toMemberId,t.amountCents);await refresh();notify('Payment marked as settled ✓')}}>Mark as paid</button></article>)}</section></main>{modal&&<Modal title="Add an expense" onClose={()=>setModal(false)}><ExpenseForm members={vibe.members} onSave={async(e:Expense)=>{await saveLiveExpense(membership,e);setModal(false);await refresh();notify('Expense added ✓')}}/></Modal>}{toast&&<div className="toast"><Check/>{toast}</div>}</>};return <><Header/><main className="page"><section className="welcome"><div><p className="eyebrow">PRIVATE FRIENDS PILOT</p><h1>Hey, Alex 👋</h1><p>Plan good times and split the tab fairly.</p></div><button className="primary" onClick={()=>go('/create')}><Plus/> Create a Vibe</button></section>{vibes.length?<section className="vibe-grid">{vibes.map(v=><VibeCard key={v.id} vibe={v}/>)}</section>:<div className="empty"><span>👀</span><h2>No vibes yet</h2><p>Create one and bring the crew together.</p><button className="primary" onClick={()=>go('/create')}>Create a Vibe</button></div>}</main></>}
function CreateLive({onDone}:{onDone:(m:StoredMembership)=>void}){const [name,setName]=useState(''),[location,setLocation]=useState(''),[date,setDate]=useState(''),[busy,setBusy]=useState(false),[error,setError]=useState('');return <><Header back/><main className="page form-page"><p className="eyebrow">NEW PLAN</p><h1>Create a Vibe</h1><form className="stack" onSubmit={async e=>{e.preventDefault();setBusy(true);try{const m=await createLiveVibe({id:'',name,emoji:'🍻',type:'Drinks',when:date,startsAt:date,location,description:'',currency:'AUD',maxMembers:10,status:'active',members:[],expenses:[],settlements:[],activity:[]});onDone(m)}catch(x){setError(x instanceof Error?x.message:'Couldn’t create vibe')}finally{setBusy(false)}}}><label>Vibe name<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Friday Drinks"/></label><label>When<input required type="date" value={date} onChange={e=>setDate(e.target.value)}/></label><label>General location<input required value={location} onChange={e=>setLocation(e.target.value)} placeholder="South Bank"/></label>{error&&<p className="form-error">{error}</p>}<button disabled={busy} className="primary full">{busy?'Creating…':'Create my Vibe'} <ChevronRight/></button></form></main></>}
function JoinLive({token,onDone}:{token:string;onDone:(m:StoredMembership)=>void}){const [preview,setPreview]=useState<{name:string;emoji:string;location:string}|null>(null),[name,setName]=useState(''),[error,setError]=useState('');useEffect(()=>{previewInvite(token).then(setPreview).catch(e=>setError(e.message))},[token]);return <><Header back/><main className="page join-page">{preview?<><div className="join-icon">{preview.emoji}</div><p className="eyebrow">YOU’VE BEEN INVITED</p><h1>Join {preview.name}</h1><p className="lede">{preview.location}</p><label>Your name<input value={name} onChange={e=>setName(e.target.value)} placeholder="Enter your name"/></label><button className="primary full" onClick={async()=>{try{onDone(await joinLiveVibe(token,name))}catch(e){setError(e instanceof Error?e.message:'Couldn’t join')}}}>Join the Vibe</button></>:<Loading/>}{error&&<p className="form-error">{error}</p>}<p className="private-note"><ShieldCheck/>Private invite · No Google login required</p></main></>}
